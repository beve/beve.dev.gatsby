import PropTypes from "prop-types"
import React from "react"

const Menu = ({ entries }) => <></>

Menu.propTypes = {
  entries: PropTypes.string,
}

Menu.defaultProps = {
  entries: ``,
}

export default Menu
